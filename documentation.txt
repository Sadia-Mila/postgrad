Postgres setup

1/npm install -g pnpm (Install pnpm)=> pnpm init
2/npm install -g typescript for install typescript
3/ For config File => tsc --init
4/ For Typescript a src folder has to create in the root and within src make a file named index.ts
    write a console.log("Hello World")
5/ tsconfig.json => File layout => uncomment "rootDir": "./src",
    "outDir": "./dist",
6/  For Node js git ignore File run ==>> pnpm dlx gitignore Node
7/ install package.json by using pnpm init -y.    
8/ Then change package.json. In script put start inplace of test  and in the right side write 
     "node dist/index.js"     and under it write     
     "build": "tsc -p .",      Change all of these
9/ pnpm i tsc-watch -D for dev dependancy 
    like this 
    "scripts": {
    "start": "node dist/index.js",
    "build": "tsc -p .",
    "dev": "tsc-watch --onSuccess \"node dist/index.js"
  },
10/ Then run pnpm run dev for server restart then you can see "hello world"
11/ instal express by using => pnpm i express     also need type: pnpm i @types/express -D and also run: pnpm i @types/node -D
12/ in tsconfig.json uncomments the following for node.js:   
    "lib": ["esnext"],
    "types": ["node"],
13/ in tsconfig.json in recommended options there is 
      "verbatimModuleSyntax": true,    make it false.
14/ install docker

15/ make file named docker-compose.yml in the root then run 
and put below in the file===>>>
services:
  postgresdb:
    image: postgres:latest
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: supperauth
    volumes:
      - 'postgres_data:/var/lib/postgresql/data'

volumes:
  postgres_data:

15/ then run ==>> docker compose up  in tarminal
16/ docker ps for check running container
17/ docker compose up -d : for docker in background
https://app.eraser.io/workspace/eSCluAVh1LBjRlNUx4Pr?origin=share

===Docker Setup End ===
============================
15/ Go to drizzle doc => Maintain Folder and file => Run the pnpm comments
16/ make .env file as documentations=>DATABASE_URL= postgresql://postgres:postgres@localhost:5432/supperauth => in doc step 2
17/ Inside the DB make a file called index.ts==In doc step 3
18/ Go to Schema.ts =>Step 4 but small differance. I prefer cammleCase but DB prefer shank_case
19/ Go to drizzleconfig.js file => step 5 as it is 
20/ Go to package.json in script section => "studio": "drizzle-kit studio" for check or see the DB 
21/ Run the comment pnpm studio for check browser ui.
22/ now i need to migrate the database but at 1st need to genarate for check then push so again go to package.json file add another script "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"                  
23/ run the comment pnpm db:generate file sql genarate and for migrate pnpm db:migrate 
==== Drizzle Setup End ===

===Auth ====
24/ instal zod for dto and type script validation ===>>> pnpm install zod 
25/ make a folder in named app under sec then make a fonder named auth under app
26/ under app make files controller.ts, models.ts, route.ts




Postgre is giving data in table table format as like as row and column whereas in mongodb is giving data in Json format

app =>> ODM(object data mapper)/mongoose (convertor)   ==>>Mongodb   (Hosting in MongoDBAtlas)
app =>> ORM(Object Relation mapper)/Drizzel (convertor) ==>>PostgreSQL  (Can be Hosted in Suparbase, AWS RDMS, Neao DB, Postgre Install, Docker-VM-Postre Y)

converor list of Data base are : Prisma, Drizzle ORM, Type ORM, MOngoose, Django, Entry Framework etc. 

ProstGreSQL is not only for js. It is also for Python, Rust, Next.js etc. 

mongoose holo ODM Library.